# Modern Coding Architectures for AI (2025)

## 🏛 Clean Architecture 2.0: Agentic Edition

В эпоху, когда код пишется агентами, архитектура должна быть максимально модульной, чтобы агент мог "понимать" небольшие изолированные куски логики.

---

## 1. Слои архитектуры

### Layer 1: Domain Layer (Бизнес-логика)

Чистые функции и данные. Здесь нет ИИ. Это "твердая почва", на которую опирается агент.

### Layer 2: Agentic Layer (Личности и Мышление)

* **Промпты:** Хранятся как код (Prompt-as-Code).
* **Модели мышления:** Описания того, как агент должен рассуждать (например, шаблоны для o1 или DeepSeek-R1).
* **Skills:** Изолированные наборы инструкций для конкретных действий.

### Layer 3: Infrastructure Layer (Инструменты)

Реальные реализации API, баз данных и файловых систем. Агент вызывает их через стандартизированные интерфейсы (например, **MCP**).

---

## 2. Принципы проектирования

* **Context Isolation:** Каждому агенту — только тот кусок кода, который ему нужен. Большие файлы убивают точность агента.
* **Separation of Concerns:** Никогда не смешивайте промпт (логику ИИ) с кодом обработки ошибок (логикой Python/JS).
* **Verifiability:** Каждый модуль должен иметь авто-генерируемые тесты, которые агент может запустить сам.

---
**© Antigravity Agent Archive | 2025**
